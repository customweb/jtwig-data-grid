<th {{ columnHeader.dynamicAttributes|join(' ') }}>
	{{ columnHeader.title }}
	{% if columnHeader.sortable %}
		<div class="sorting {{ columnHeader.orderingClass }}">
			<a class="ajax-event" href="{{ columnHeader.sortingUrl }}"><span class="sorting-button">&nbsp;</span></a>
		</div>
	{% endif %}
</th>